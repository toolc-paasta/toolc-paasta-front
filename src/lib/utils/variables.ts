import { Dimensions } from "react-native";

const { width, height } = Dimensions.get("window");
export const W_WIDTH: number = width;
export const W_HEIGHT: number = height;

export const MODAL_PADDING: number = 10;
